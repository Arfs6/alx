#include "main.h"

/**
 * mul - multiply two numbers
 * @a: an integer to multiply
 * @b: secont integer to multiply
 *
 * Return: result of the multiplication
*/
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
