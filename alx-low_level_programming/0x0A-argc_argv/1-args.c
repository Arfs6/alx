#include <stdio.h>

/**
 * main - print number of farguments
 * @argc: arguments count
 * @argv: arguments string
 *
 * Return: always (0)
*/
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%i\n", (argc - 1));
	return (0);
}
