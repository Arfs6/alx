#include <stdio.h>

/**
 * main - print name of the executable
 * @argc: number of arguments
 * @argv: arrays of  string of arguments
 *
 * Return: always 0 (success
*/
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
